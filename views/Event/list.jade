extends ../layout
block content
  each event in events
    article
      h3= event.doi
      p= event.story
      small
        em= event.accessed.calendar()
        span: #{event.profession} from #{event.location}
